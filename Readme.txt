README:

-Criar uma base de dados em Postgres com o nome postgres,
com um dono com o nome postgres e a password oracle. Há também um ficheiro
de backup da base de dados incluído neste ZIP com o nome SD-BD.backup

-Criar a base de dados usando o ficheiro de backup da base de dados;

-Correr o dataserver.jar, de seguida o server.jar e o client.jar;
